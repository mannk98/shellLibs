# shellLibs

## Install:
**run `as root user`**
```bash
    ./install 
```
- Command above install shellLibs to /bin/shellLibs directory and source these file (~/.bashrc) each time new bash session start.
  (run as non-root user maybe make some functions not work because of permission problem) 
